# Contributing to ModelSentinel

感谢你帮助 ModelSentinel 覆盖更多 AI 客户端与线路。

## 开始开发

1. Fork 仓库并创建主题分支。
2. 使用 `swift build` 验证编译。
3. 使用 `./script/build_and_run.sh --verify` 验证应用可启动。
4. 提交 Pull Request，并说明被检测客户端、配置来源、证据等级和隐私影响。

## 适配器要求

- 不提交真实 Token、账号信息、对话、私有域名或未匿名化配置。
- 不读取凭据值；如确有必要，只判断字段是否存在。
- 安装、运行、配置、响应、行为证据必须分层展示。
- 没有真实响应证据时，不得将配置模型描述为实际响应模型。
- 新增路径或配置键时，请附上官方文档或可公开验证的依据。
- 对专有客户端内部文件的支持应采用容错读取，不能假设未公开格式长期稳定。

提交代码即表示你同意按仓库的 MIT License 授权该贡献。
